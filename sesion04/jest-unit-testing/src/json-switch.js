export const datos = (id) => {
    switch (id) {
        case 1:
            return { nombre: "Dans" }

        case 2:
            return { apellido: "" }

        case 3:
            return { curso: "js-fullstack" }
    }
}